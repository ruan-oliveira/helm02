apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-example-web-monitoring.fullname" . }}-test-connection"
  labels:
    {{- include "app-example-web-monitoring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-example-web-monitoring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
